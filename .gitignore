# Dart/Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# pubspec.lock은 커밋한다 — 앱은 라이브러리와 달리 의존성 버전을 고정해야
# 스토어에 올린 빌드를 나중에 그대로 재현할 수 있다(결제 SDK가 들어가면 특히).

# gen-l10n 생성물
lib/l10n/app_localizations*.dart

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# 골든 실패 시 생성되는 diff 이미지 — 실행할 때마다 바뀌는 노이즈다
test/failures/

# 릴리스 서명 — 절대 커밋 금지 (키/비밀번호 유출 = 업로드 키 탈취)
android/key.properties
*.jks
*.keystore

# Kotlin 빌드 세션 캐시
android/.kotlin/
